#include <stdio.h>

int main() {
    int arr[] = {12, 45, 67, 89, 34, 67, 12, 45};
    //int size = sizeof(arr) / sizeof(arr[0]);
    int max ;
    //melakukan proses pencarian nilai terbesar dari array

    for ( int i = 0; i < arr[i]; i++)
    {
        if (arr[i] > max)
        {
             printf("Nilai terbesar dari array adalah : %d\n", max);
            //max = arr[i];
                }
        
       //printf("Nilai terbesar dari array adalah : %d\n", max);
    }
    

    //printf("Nilai terbesar dari array adalah : %d\n", max);

    return 0;
}